This is my attempt at creating a basic neural network from scratch using only python, without addition AI supporting modules. 

The goal will be to implement this to identify numbers in the MNIST dataset
